import { defineConfig } from "rspress/config";

export default defineConfig({
  title: "All Notes Documentation",
  description: "Documentation for all notes in All-Notes folder",
  // 明确指定静态资源目录（可选，若图片在 docs 子目录中）
});
